import React, { type FC } from 'react'
import BookForm from '../components/BookForm'

const NewBookForm = () => {
  return <BookForm title='Publish New Book' submitBtnTitle='Publish Book'/>
}

export default NewBookForm
